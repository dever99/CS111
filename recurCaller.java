public class recurCaller 
{
    public static void main(String[]args)
    {
        int result = recursion.rNumDig(3471); 
        StdOut.println(result); 
        // void methods dont have return types, so you can just call the (file name).(function name(variable))

        recursion.rPrintNum(10);
        recursion.rCon(8); 
        
    }
    
}
